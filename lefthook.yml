pre-commit:
  parallel: true
  jobs:
    - run: pnpm eslint --quiet --fix '{staged_files}'
      glob: "*.{ts,tsx}"
      stage_fixed: true
    - run: pnpm prettier --ignore-unknown --write '{staged_files}'
      stage_fixed: true
    - run: pnpm check
      glob: "*.{ts,tsx}"
